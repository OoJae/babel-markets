// /app top hero strip. Anton headline with a Cormorant italic em accent in
// Pompeii red. Continuous with the landing's typography.

export function AppHero() {
  return (
    <section className="app-hero">
      <h1>
        Paste a tongue. <em>Mint a market.</em>
      </h1>
      <p>
        The agent loop, in full. Detect, translate, judge, synthesize, critique,
        dedup, decide. Every step is gated behind a Circle Gateway Nanopayment
        on Arc testnet, so the per-step USDC receipts on this page are real.
      </p>
    </section>
  );
}
